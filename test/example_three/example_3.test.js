import example3EntryPoint from '../../src/example_three/example_3';

describe('can you implement a suite of tests to describe what this function does?', () => {
  test('they should form a type of living documentation, Given, When, Then', () => {
    example3EntryPoint([]);

    expect(true).toBe(true);
  });
});
